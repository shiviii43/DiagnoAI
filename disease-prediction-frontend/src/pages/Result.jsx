import { useLocation } from "react-router-dom"
import PageTitle from '../components/PageTitle'
import DiseaseTitle from "../components/DiseaseTitle"
import DiseaseScore from "../components/DiseaseScore"

const Result = () => {
    
    /*
    const location = useLocation()
    const {predictionData} = location.state
    */
    const predictionData = {
        "disease" : "diabetes",
        "confidence" : 98,
        "prediction" : 1
    }
    const title = "Prediction Results"
    const para = "These are the results which are generated by the AI based models."
    return(
        <div className="h-1/2 w-200 mx-auto py-10 px-15 my-10">
            <PageTitle title={title} paragraph={para}/>
            <DiseaseTitle disease={predictionData.disease} prediction={predictionData.prediction}/>
            <DiseaseScore disease={predictionData.disease} confidence={predictionData.confidence} prediction={predictionData.prediction}/>
        </div>
    )
}
export default Result